package demo.helpers;

public class UserHelper {
	
	public static boolean isSignedIn(String userDomain, String userEmail) {
		//TODO In the future, we need to ad some real logic of checking if the user is logged in
		return true;
	}
	
	public static boolean isLoggedIn(String userDomain, String userEmail) {
		//TODO In the future, we need to ad some real logic of checking if the user is logged in
		return true;
	}

	public static boolean isAdmin(String userDomain, String userEmail) {
		//TODO In the future, we need to ad some real logic of checking if the user is logged in
		return true;
	}
	
	public static boolean isManager(String userDomain, String userEmail) {
		//TODO In the future, we need to ad some real logic of checking if the user is logged in
		return true;
	}
}
